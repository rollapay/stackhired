run = "npm run dev -- --port 3000 --hostname 0.0.0.0"
entrypoint = "app/page.tsx"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npm run start -- --port 3000 --hostname 0.0.0.0"]
build = ["sh", "-c", "npm run build"]

[[ports]]
localPort = 3000
externalPort = 80
